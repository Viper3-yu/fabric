# 项目验收记录

验收日期：2026-07-29

## 结论

“迹信”物流追踪系统已完成 Go 重构：浏览器端继续使用 React/TypeScript，HTTP API、Fabric Gateway 适配器和 Hyperledger Fabric 智能合约均已改为 Go。旧 TypeScript API、旧 TypeScript 链码、Node Fabric 依赖和过期截图已清理。

本次不仅验证了演示账本。新的 Go 链码已部署到本机双组织 Fabric 2.5.16 测试网络，并通过 Go API 完成建单到签收的真实链上闭环；API 重启后仍能读取相同运单、事件和交易 ID。

## 自动化验证

| 范围           | 结果 | 覆盖内容                                                      |
| -------------- | ---- | ------------------------------------------------------------- |
| 类型检查       | 通过 | React/TypeScript 类型检查；Go API 与 Go 链码编译检查          |
| 单元与组件测试 | 通过 | Web 8 个测试文件、17 项测试；Go API、演示账本和 Go 链码测试   |
| 演示闭环       | 通过 | `TestClosedLoop` 覆盖建单、流转、异常、签收、公开查询与持久化 |
| 静态检查       | 通过 | TypeScript 编译检查与 `go vet`                                |
| 格式检查       | 通过 | Prettier 与 `gofmt`                                           |
| 生产构建       | 通过 | shared、Web、Go API、Go 链码均构建成功                        |
| Go 依赖打包    | 通过 | `GOWORK=off go mod vendor`，兼容 Fabric `deployCC`            |

本轮验证使用官方 Go 1.26.5 Windows 便携运行时。项目声明的最低 Go 版本仍为 1.23，并已移除测试中对 Go 1.24 新 API 的依赖。

## 真实 Fabric 闭环

- Fabric：2.5.16
- 通道：`logisticschannel`
- 链码：`logistics`
- 链码版本/序列：`1.0 / 1`
- 组织：`Org1MSP`、`Org2MSP`
- API 网络状态：`mode=fabric`、`isDemo=false`、`health.status=ok`
- 验收运单：`JX20260729254558`
- 最终状态：`RECEIVED`
- 链上事件：9 条
- 有效修改交易：9 笔，全部返回 64 位十六进制 Fabric 交易 ID
- 最终交易 ID：`e93de178fe1024b22a5086e2d86e8d027dd2ff07518b942a793963b5574f7a87`
- API 重启复验：通过，运单状态、9 条事件及最终交易 ID 均保持不变

该结果证明的是本机 Fabric 测试网络上的真实提交与持久化，不代表生产网络部署或生产级运维验收。

## 清理记录

- 删除 `apps/api` 下的 TypeScript/Express/Vitest 实现。
- 删除 `chaincode/logistics` 下的 TypeScript/npm 链码实现。
- 从 pnpm workspace 和锁文件移除 Node Fabric 依赖。
- 删除整个 `output/playwright` 历史截图目录：4 张旧版受控验收图及 51 张未跟踪的本地 UI 截图均已无引用；需要新的视觉证据时应重新运行浏览器验收生成。
- 忽略并清理 Go 缓存、构建产物、Fabric 本机身份配置等可再生文件。
- 保留 React 前端、共享 TypeScript 类型、Fabric 测试网络脚本和项目文档。

## 未纳入本轮结论

- 未重新执行浏览器视觉回归或生成新截图。
- 未验证生产 Fabric 网络、多节点高可用、密钥托管、监控告警和灾备。
- 未接入真实 IoT 温度传感器；当前温度由检查点人工录入，并保留越界异常逻辑。
