# 项目验收清单

## 构建与测试

- [x] `pnpm install` 成功
- [x] `pnpm typecheck` 成功
- [x] `pnpm build` 成功
- [x] `pnpm test` 成功
- [x] API 闭环测试从建单运行到签收
- [x] 错误签收码、非法状态跳转和越权调用均被拒绝

## 浏览器功能

- [x] 登录和退出
- [x] 四种角色只能看到允许的操作
- [x] 发货方创建运单
- [ ] 发货方取消待接单运单（功能与自动化覆盖，提交前建议再手动演示一次）
- [x] 承运方接单、揽收、更新节点、异常处理和送达
- [x] 收货方使用签收码确认收货
- [x] 公开查询返回脱敏轨迹
- [x] 交易证据可查看和复制
- [x] 加载、空、错误和成功状态完整
- [ ] 360px、768px 和 1440px 宽度无横向溢出（已实测 390px 与 1440px）
- [ ] 键盘可操作、焦点清晰和减少动态效果设置（代码已实现，提交前建议用键盘再复验）

## Fabric 网络

- [x] Docker Desktop 正常运行
- [ ] `pnpm fabric:bootstrap` 成功
- [x] `pnpm fabric:up` 成功
- [x] Org1 和 Org2 peer 均加入 `logisticschannel`
- [x] `logistics` 链码在通道上已提交
- [x] API 使用 `LEDGER_MODE=fabric` 启动
- [x] 修改操作返回真实 Fabric 交易 ID
- [ ] Org1 不能执行承运动作，Org2 不能创建或签收运单
- [x] `GetShipmentHistory` 返回连续历史
- [ ] `pnpm test:fabric` 通过（建单到签收的真实链上闭环，需 Docker 测试网络运行中）

## 提交目录

- [x] 前端、后端、SDK 和智能合约源码齐全
- [x] README、设计方案、项目介绍、演示流程和验收记录齐全
- [x] `.env`、私钥、钱包和 `node_modules` 已配置忽略（最终压缩包仍需确认未包含）
- [ ] 团队成员和贡献值已按实际填写
- [ ] 答辩 PPT 由团队补充
- [ ] 演示讲解视频由团队录制
