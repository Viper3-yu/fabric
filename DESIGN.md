# 迹信设计系统

## 设计方向

迹信是一套可信物流取证控制台。界面需要让用户迅速判断货物在哪里、当前责任属于谁、下一步需要做什么，以及一条记录是否能够被复核。

整体语言来自运输清单、交接单和链上回执，而不是科技展厅。公开服务负责解释价值，业务工作台负责高密度操作，证据模块负责建立反差和可信感。

## 设计原则

1. 事实先于装饰。路线必须带节点、状态或时间，不能只是背景曲线。
2. 动作先于统计。异常、待处理和当前责任始终早于总量展示。
3. 证据保持克制。交易 ID、摘要、账本模式和时间戳使用等宽数字，不使用霓虹、字符滚动或持续发光。
4. 隐私边界可见。公开信息、脱敏信息和链下原件必须通过文案和层级明确区分。
5. 同一套几何贯穿全站。表面使用 4px 圆角，控件使用 2px 圆角，胶囊只用于状态标签。

## 视觉基调

- 画布：冷白与矿物灰，用于稳定的操作背景。
- 文字：墨黑与炭灰，形成清晰扫描路径。
- 主色：森林绿，只用于主动作、焦点、已验证路径和品牌标记。
- 证据面板：深墨绿，用于链路、账本和关键回执。
- 语义色：蓝色表示运输状态，橙色表示等待，红色表示异常。语义色不能替代主操作色。
- 深度：静态表面只使用 1px 边框和表面色差。只有弹层可以使用单层阴影。

## 网格与布局

- 基础单位：4px。
- 桌面：16 列，内容轴最大 1312px，左右 gutter 32px。
- 平板：8 列，左右 gutter 24px。
- 移动：4 列，左右 gutter 16px。
- 导航最高 72px，保持单行。
- 页面标题、说明和主动作共享同一条水平基线。
- 后台标题左对齐。只有查询单任务、空状态、成功状态和关键核验结论可以居中。

## 字体

- 中文正文：`Microsoft YaHei UI`、`PingFang SC`、系统无衬线字体。
- 拉丁展示：`Cabinet Grotesk` 字形方向，运行时使用本地可用的几何无衬线回退，避免外部字体请求阻塞。
- 数据：`IBM Plex Mono` 字形方向，运行时使用 `Cascadia Mono`、`SFMono-Regular`、`Consolas` 回退。
- 标题使用中等字重，不使用超粗营销字体。
- 运单号、交易 ID、区块号、摘要和时间戳启用等宽数字。

## 组件

### 导航

导航固定在页面顶部，使用冷白实色和底部分隔线，不使用悬浮胶囊、厚阴影或大面积毛玻璃。当前项以森林绿下边线和文字强调。

### 按钮

- 主按钮：森林绿实底。
- 次按钮：透明底与墨绿边框。
- 危险按钮：仅在取消和异常操作中使用红色。
- 按钮不做明显抬升或弹跳。按下时最多向下移动 1px。

### 表单

输入控件使用清晰的 1px 边框和近白底色。聚焦时显示 2px 森林绿焦点环。标签始终可见，不只依赖占位文字。

### 表面

普通卡片不使用阴影。相邻指标使用连体网格而不是重复悬浮卡片。证据面板可以使用深墨绿底色，但内部信息仍以细线划分。

### 状态

状态标签可以使用胶囊几何。装饰性圆点禁止出现，网络在线圆点必须配合可读状态文字。

### 路线

路线首次进入时绘制一次，随后静止。节点必须对应创建、交接、运输、异常或签收等真实含义。当前位置使用实心节点，未到达位置使用空心节点。

## 动效

动效只表达三件事：

1. 路径连接。
2. 节点确认。
3. 状态切换。

时间规范：

- 即时反馈：80ms。
- 微交互：120ms。
- 控件状态：180ms。
- 面板进入：240ms。
- 页面进入：360ms。
- 公开页首屏：520ms。

缓动规范：

- 标准：`cubic-bezier(.2, 0, 0, 1)`。
- 进入：`cubic-bezier(0, 0, .2, 1)`。
- 离开：`cubic-bezier(.4, 0, 1, 1)`。
- 强调：`cubic-bezier(.16, 1, .3, 1)`。

限制：

- 后台页面最长动效不超过 240ms。
- 列表和表格行不做逐条入场。
- 公开页最多保留一个跑马信息条。
- 不使用滚动钉住、视差、磁吸按钮、3D 倾斜和持续呼吸。
- `prefers-reduced-motion: reduce` 下直接显示最终状态，并停止跑马。

## 明暗主题

主题在整页范围自动切换。浅色主题使用冷白画布，深色主题使用深炭绿画布。森林绿在深色主题中提高亮度，但仍保持单一品牌主色。禁止在同一页面中拼接互不相干的明暗主题。

## 响应式

- 公开页桌面采用文字与物流影像两栏，移动端先展示查询任务，再展示约 220px 高的证据影像。
- 后台概览桌面采用稠密两列，移动端顺序为待处理、核心指标、网络证据、最近运单。
- 长表单移动端使用清晰的底部操作区，触控目标不小于 44px。
- 表格在移动端转为业务卡片，不进行水平压缩。

## 禁止事项

- 不混入多套品牌蓝、荧光绿或紫色渐变。
- 不使用厚重圆角、层层阴影和玻璃拟态。
- 不将所有标题居中。
- 不将所有内容放进独立卡片。
- 不让绿色和红色同时承担主按钮角色。
- 不在数据更新时重复播放整页揭幕动画。
- 不使用没有业务含义的光点、扫描线和循环漂浮。
